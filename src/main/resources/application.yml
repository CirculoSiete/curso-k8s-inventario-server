grpc:
 enabled: true
 servers:
   -
     address: 127.0.0.1
     port: 7000

spring:
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
  datasource:
    url: "jdbc:postgresql://localhost:5432/mydb"
    username: "postgres"
    password: "example"
    driver-class-name: "org.postgresql.Driver"
  jpa:
    generate-ddl: true